# Weekly 27/07/2026

## Mục tiêu (từ meeting)

- Dựa vào bộ gia phả quốc ngữ đang có (rất nhiều) → huấn luyện 1 mô hình
- Thu thập dữ liệu

---

## Đã làm

### Pipeline thu thập + gán nhãn (`label_studio_pipeline/`)

- Crawl **vietnamgiapha.com**: Phả ký + Sơ đồ (`pha_ky.txt`, `pha_he.json`)
  - Dải 1: `tree_id` **100–200**
  - Dải 2: `tree_id` **201–1500**
  - **1.263** thư mục crawl; **189** gia phả hợp lệ (Phả ký ≥200 ký tự + sơ đồ có node)
- Pre-annotate bằng **Gemini** (NER + quan hệ) → import **Label Studio**
  - Project: **Family Tree NER+RE** (id **3**)
  - **38 task** đã import (8 pilot + 30 batch 2)
  - Tổng ~196k ký tự Phả ký; ~971 entity, ~214 relation (pre-anno)
- Đối chiếu tên Gemini vs sơ đồ (`cross_check.json`) — khớp ~31%

### Schema nhãn (cho huấn luyện sau)

| Entity | Relation |
|--------|----------|
| `PER_NAME`, `GENERATION`, `DATE`, `ORDER`, `LOC` | `FATHER_OF`, `MOTHER_OF`, `SPOUSE` |

1 task = **full Phả ký** (không chunk). Sơ đồ dùng phân tích/cross-check, không gán trên LS.

### Tài liệu

- Plan: `planning/label_studio_pipeline_plan.md`
- Hướng dẫn gán nhãn (SSOT): `label_studio_pipeline/HUONG_DAN_GAN_NHAN.md`
- Thống kê: `label_studio_pipeline/THONG_KE.md`
- Cập nhật `PROJECT.md` §9

### CLI chính

```bash
python -m label_studio_pipeline.crawl_corpus --start 100 --end 200
python -m label_studio_pipeline.label_and_import --pilot-file data/vgp_corpus/batch_trees.json --cross-check
```

---

## Tiếp theo

- [ ] **Gold annotate** trên Label Studio (sửa pre-anno Gemini → Submit) — mục tiêu ≥10 task
- [ ] Export JSON gold từ LS → chuẩn bị dataset huấn luyện
- [ ] Crawl thêm batch (1501+, ~30 cây/lần) nếu cần mở rộng corpus
- [ ] Review prompt / task 0 entity (145, 263, 373, 430)
- [ ] Huấn luyện mô hình NER+RE từ gold data
