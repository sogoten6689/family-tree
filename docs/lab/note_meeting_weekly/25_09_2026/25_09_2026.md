# Việc làm 25/9/2026 — theo yêu cầu sau họp 14/9

Nguồn gốc: tiếp nối [`14_09_2026.md`](../14_09_2026/14_09_2026.md) +
[`phan_tich_hop_14_09_2026.md`](../14_09_2026/phan_tich_hop_14_09_2026.md).
Yêu cầu hôm nay: "tạo task làm đi, tìm repo git về chạy thử, nâng cấp, sử
dụng thử, đánh giá / tìm thêm dữ liệu, làm sạch, phân tích, báo cáo".

## Task backlog đã tạo

| # | Task | Trạng thái |
|---|------|-----------|
| 1 | Săn tìm mô hình/dataset gia phả Hán (TQ) có sẵn (mục 5 họp 14/9) | Đang chạy (agent nền) |
| 2 | Clone + chạy thử repo tiềm năng nhất | Đang chạy (agent nền) |
| 3 | Đánh giá khả năng kế thừa/transfer vào pipeline hiện tại | Đang chạy (agent nền) |
| 4 | Đề xuất phương án nâng cấp pipeline (proposal, chưa merge) | Chờ kết quả #1-3 |
| 5 | Tìm thêm dữ liệu gia phả lâu năm (gốc Hán Nôm) | **Blocked** — thiếu `FIRECRAWL_API_KEY` thật trong môi trường session (chỉ có `.env.example`) |
| 6 | Làm sạch + phân tích dữ liệu OCR đã vote (`runs/*.vote.json`) | **Blocked** — dataset thật nằm ở repo riêng `hannom-bilingual-dataset`, ngoài phạm vi repo được cấp quyền cho session này (chỉ `sogoten6689/family-tree`) |
| 7 | Tổng hợp 5 câu hỏi mở cần thầy xác nhận | Đã tổng hợp lại từ `phan_tich_hop_14_09_2026.md` |
| 8 | Báo cáo phiên làm việc | File này |

## Vì sao #5 và #6 bị blocked (fact, không phải giả định)

- `env | grep -i firecrawl` → rỗng. `find . -iname ".env*"` → chỉ thấy các
  file `.env.example` (research/source_discovery, nlp_family_extractor),
  không có `.env` thật chứa key. → Không thể gọi Firecrawl `/map`, `/scrape`,
  `/crawl` thật trong môi trường này.
- Dataset OCR (`runs/*.vote.json`, `hannom-bilingual-dataset/`) đã được tách
  ra repo riêng từ commit `1463633 Stop tracking data/ in the main repo`.
  Phạm vi GitHub của session hiện tại chỉ gồm `sogoten6689/family-tree`
  (xem system context) — không thấy `hannom-bilingual-dataset` trong repo
  này, nên Claude không có quyền đọc/ghi dữ liệu đó trực tiếp.
- **Cần từ user/thầy:** (a) cấp `FIRECRAWL_API_KEY` thật nếu muốn Claude tự
  chạy crawl tìm nguồn mới trong phiên làm việc trên cloud; (b) thêm repo
  `hannom-bilingual-dataset` vào scope nếu muốn Claude trực tiếp làm sạch/
  phân tích dữ liệu vote OCR.

## Việc #1–#3 (săn mô hình, chạy thử, đánh giá) — đang chạy nền

Giao cho agent nghiên cứu: tìm mô hình/dataset NLP có sẵn cho gia phả Hán
Trung Quốc (家谱/族谱) — OCR cổ văn, NER quan hệ/tên người, hoặc dựng cây
gia phả từ text — trên GitHub/HuggingFace; chọn ứng viên khả thi nhất; clone
và chạy thử thật (không chỉ đọc paper); ghi lại kết quả thật (thành công/
thất bại, lỗi cụ thể); đánh giá license + effort tích hợp vào
`nlp_family_extractor`. Kết quả sẽ được cập nhật vào file này khi agent hoàn
tất (xem mục "Cập nhật" bên dưới nếu có).

## Cập nhật — kết quả T1-T3 (agent hoàn tất, ~26 phút chạy nền)

Toàn bộ kết quả chi tiết: [`../../../planning/chinese_genealogy_model_hunt_plan.md`](../../../planning/chinese_genealogy_model_hunt_plan.md) §6 + [`research/model_survey/`](../../../../research/model_survey/).

**Tóm tắt (fact, có log thật kèm theo):**
- Khảo sát 12 ứng viên (OCR Hán cổ, NER cổ văn, KG quan hệ nhân vật, quản lý cây gia phả). **Không có ứng viên nào** giải quyết trực tiếp "gia phả Hán TQ → cây gia phả có cấu trúc" — đáng báo lại thầy như một **phát hiện** (khoảng trống học thuật tiềm năng cho luận văn), không phải một tìm kiếm thất bại.
- Ứng viên NER lý thuyết phù hợp nhất (guwen-ner/GuwenBERT — Apache-2.0, Jiayan — MIT) **không chạy thử được**: môi trường sandbox Claude Code cloud chặn egress tới huggingface.co / modelscope.cn / drive.google.com / zenodo.org (403, chính sách tổ chức) — chỉ github.com và pypi.org truy cập được.
- Ứng viên duy nhất chạy thử được thật: `CHAT_models` (kraken OCR Hán cổ, TQ, 1.7M dòng huấn luyện, license **CC BY-NC 4.0** — cấm thương mại). Kết quả: cài đặt sạch, nhưng README gốc có bug đường dẫn + code demo lỗi do kraken đổi API kể từ 2023 (repo không ghim version). Sau khi tự vá lỗi, chạy được nhưng: 459.5 giây/trang chỉ riêng segmentation (CPU), và **0% ký tự đúng** trên cả ảnh scan thật của repo lẫn câu gia phả Hán cổ tự tạo (`始祖諱文成官至知府娶阮氏生子一人諱德重`).
- Nguyên nhân gốc của kết quả 0% có 2 giả thuyết cạnh tranh chưa phân định (H1: model kém; H2 có bằng chứng gián tiếp mạnh hơn: version mismatch giữa mlmodel 2023 và kraken engine 2026) — **chưa kết luận dứt khoát**, cần 1 thực nghiệm rẻ tiếp theo (`kraken<5`) trước khi quyết định.

**Quyết định:** **không** viết proposal nâng cấp pipeline (T4) ở bước này — điều kiện chưa đủ (output 0%, license chặn thương mại, nguyên nhân chưa phân định). Việc tiếp theo hợp lệ duy nhất là thực nghiệm phân định H1/H2, giao lại cho Cursor hoặc phiên sau (xem plan T4).

**Cập nhật task tracker:** #1, #2, #3 → hoàn tất. #4 → giữ pending, mô tả cập nhật rõ điều kiện chưa đủ để bắt đầu.
