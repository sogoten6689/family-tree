# Weekly 09/08/2026 — Thực hiện kế hoạch tuần 02/08

> **Phiên làm việc:** Stratified gold · CLI export/import · review queue  
> **Project LS:** Family Tree NER+RE (id **3**) — cần LS chạy để audit/import

---

## Mục tiêu tuần (từ kế hoạch 02/08)

1. Chọn corpus stratified (S1–S4) cho human review
2. Implement CLI import synthetic → LS project riêng
3. Implement export gold từ LS (human-reviewed)
4. Audit LS ↔ tier_a
5. Chuẩn bị review queue ưu tiên

---

## Công việc đã làm

### 1. Stratified gold sample (25 doc)

- Module mới: **`select_stratified_gold.py`**
- Output: `data/gold_labels/stratified_sample.json`
- Review queue: `data/gold_labels/REVIEW_QUEUE.md`

| Stratum | Mục đích | Số doc |
|---------|----------|--------|
| **S1** | Relation-rich (≥5 rel) | 8 |
| **S2** | Medium / generalization | 7 |
| **S3** | Hard (mixed, low overlap) | 5 |
| **S4** | Held-out test | 5 |
| **Double annotation** | Cohen's κ | 5 |

**Ưu tiên review (S1 + S4):** 122, 1684, 367, 321, 310, 1630, 277, 1508, 1622, …

### 2. CLI mới

| Module | Mô tả |
|--------|--------|
| `select_stratified_gold.py` | Chọn 25 doc stratified + metadata |
| `import_synthetic.py` | Import 31 synthetic → project LS riêng |
| `export_ls_gold.py` | Export annotation human → `gold_labels/v1_human/` |
| `audit_ls_tasks.py` | Đối chiếu LS task ↔ `tier_a_trees.json` |

```bash
# Chọn stratified sample
python -m label_studio_pipeline.select_stratified_gold

# Audit LS (cần LS chạy)
python -m label_studio_pipeline.audit_ls_tasks

# Import synthetic (project riêng, không Gemini)
python -m label_studio_pipeline.import_synthetic --skip-existing

# Export sau khi review tay trên LS
python -m label_studio_pipeline.export_ls_gold
```

### 3. Cấu hình

- `.env.example`: thêm `LABEL_STUDIO_SYNTHETIC_PROJECT_ID`, `LABEL_STUDIO_SYNTHETIC_PROJECT_TITLE`

---

## Chưa chạy (cần Label Studio)

- [ ] `audit_ls_tasks` — LS không chạy (`connection refused`)
- [ ] `import_synthetic --skip-existing` — 31 task synthetic
- [ ] **Human review** trên LS — ưu tiên S1/S4 trong `REVIEW_QUEUE.md`
- [ ] `export_ls_gold` — sau khi có ≥1 annotation human

---

## Việc bạn cần làm thủ công

1. **Khởi động Label Studio** (`http://localhost:8080`)
2. **Review 10 doc đầu** trong `data/gold_labels/REVIEW_QUEUE.md` — sửa span/relation → Submit
3. Chạy:
   ```bash
   python -m label_studio_pipeline.audit_ls_tasks
   python -m label_studio_pipeline.import_synthetic --skip-existing
   python -m label_studio_pipeline.export_ls_gold
   ```

---

## Tiếp theo (tuần 3 kế hoạch)

- [ ] Double-annotate 5 doc (κ)
- [ ] Baseline B0 (rule) + B1 (Gemini) trên S4
- [ ] Sửa encoding ~23 bộ + tree 1065
- [ ] (Tuỳ chọn) Tier B batch 20 — cap Gemini

---

## Liên quan

- [02_08_2026.md](./02_08_2026.md) — gold, Tier A, synthetic
- [label_studio_data_expansion_analysis.md](../planning/label_studio_data_expansion_analysis.md) §6, §12
- [REVIEW_QUEUE.md](../data/gold_labels/REVIEW_QUEUE.md)
